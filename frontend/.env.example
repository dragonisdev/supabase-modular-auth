# Frontend Environment Variables
# Leave empty to use the recommended same-origin proxy.
NEXT_PUBLIC_API_BASE_URL=

# Backend origin used by Next.js server-side rewrites.
# Browser requests use /auth/* and /api/admin/* so cookies stay first-party.
FRONTEND_PROXY_TARGET=http://localhost:3000
